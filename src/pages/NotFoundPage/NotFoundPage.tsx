const NotFoundPage = () => {
  return (
    <div>
      <h2 style={{ textAlign: 'center' }}>
        Вибачте, сталося недорозуміння. Такої сторінки не існує, лалка)))
      </h2>
    </div>
  );
};

export default NotFoundPage;
